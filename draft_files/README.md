# Example config files for `draft` launcher

These files typically need to go in `/etc/draft/...`.

* Make sure the path to the `keywriter` binary in `04-keywriter` matches where you actually copied it.
